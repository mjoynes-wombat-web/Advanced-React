import React from 'react';
import PropTypes from 'prop-types';

import Items from '../components/Items';

const Home = props => (
  <div>
    <Items />
  </div>
);

Home.propTypes = {

};

export default Home;
